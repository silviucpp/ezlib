{pre_hooks, [{"(linux|darwin)", compile, "make compile"}]}.
{post_hooks, [{"(linux|darwin)", clean, "make clean"}]}.

{artifacts, ["priv/ezlib_nif.so"]}.

{project_plugins, [rebar3_hex]}.

{deps, [

]}.
